```C#
c.wl(string text) = Console.WriteLine()
c.w(string text) = Console.Write()
c.r(string ask, string pref) = Console.ReadLine("")
c.border(string borderTyp) = "adds a border to  the console for nicer code managing"
c.color(string color) = ConsoleColor.Forground() && ConsoleColor.Background()
```
